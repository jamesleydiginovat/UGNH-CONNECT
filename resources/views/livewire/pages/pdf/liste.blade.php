<section>
    {{-- <h2>Liste du personnel</h2>

    <p>Type: {{ $type }}</p>
    <p>Date: {{ $date }}</p>

    <table border="1" width="100%">
        <tr>
            <th>Nom</th>
            <th>Type</th>
        </tr>

        @foreach($personnels as $p)
            <tr>
                <td>{{ $p->name }}</td>
                <td>{{ $p->type }}</td>
            </tr>
        @endforeach
    </table> --}}
</section>